import { ShoppingCartModalDirective } from './shopping-cart-modal.directive';

describe('ShoppingCartModalDirective', () => {
  it('should create an instance', () => {
    const directive = new ShoppingCartModalDirective();
    expect(directive).toBeTruthy();
  });
});
