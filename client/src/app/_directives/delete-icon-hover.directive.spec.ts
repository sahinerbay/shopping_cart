import { DeleteIconHoverDirective } from './delete-icon-hover.directive';

describe('DeleteIconHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new DeleteIconHoverDirective();
    expect(directive).toBeTruthy();
  });
});
